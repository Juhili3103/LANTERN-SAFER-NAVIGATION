# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Node (if you add tooling later)
node_modules/
npm-debug.log*

# Secrets — never commit these. LANTERN itself needs none:
# only the PUBLIC Supabase key goes in js/config.js.
.env
.env.*
*.pem
